# openbsd-cd-uefi-patch

Patched Makefile with support for creating install.iso file with UEFI support

### How to
1. Run `./setup.sh` to download required file sets and files
2. Run `make`
3. Burn efiinstall.iso to cd or dvd